# World State

This controlled directory stores author-approved locations, factions, systems, technologies, and items using stable IDs.

Draft extraction and agent proposals do not belong here. Promote only an explicitly approved delta with provenance.

Use `_templates/` for locations, factions, systems/technologies, and items/ships. Every promoted card must record provenance, truth/knowledge layer where relevant, current state, reveal constraints, and superseded facts.
